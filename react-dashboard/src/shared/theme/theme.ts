import { createTheme } from '@mui/material'

export const theme = createTheme({
  palette: {
    background: {
      default: '#f7f8fc',
    },
    primary: {
      main: '#2563eb',
    },
  },
  shape: {
    borderRadius: 12,
  },
})
